---
name: save-to-brain
description: "MERGED into /end-of-session on 1 September 2026 — pointer stub only. All MORPh capture (builds, decisions, tools, rules, notes into `1 MORPh/`, seat brains, Rupert's overview, log lines) now lives in /end-of-session Step 4. If Dean says \"save to brain\", \"brain this\", \"log this\", \"record this\", \"capture this for morph\" or \"write this up\", invoke /end-of-session and run its Steps 4–6 only (file, show Dean the exact markdown, apply and log) — not the full close-out loop. This stub exists so older references resolve; do not follow any older copy of this file."
metadata:
  version: 2.0.0
---

# Save to Brain — merged into `/end-of-session`

**This skill was merged into `/end-of-session` on 1 September 2026.** The MORPh filing procedure — the routing table, the three-stage fan-out (seat brains → Rupert's overview → shared brain), the log-line convention and the picker convention — now lives in **one place**: `/end-of-session`, **Step 4 (File the session's substance into the brain)**.

## What to do when you land here

- **Dean said "brain this" / "save to brain" / "log this" mid-session** → invoke `/end-of-session` and run **Steps 4–6 only** (file, show Dean, apply and log). Do not run the scan, the ledger, or the full close-out loop.
- **Another skill handed off here** (`/granola`, `/finish-project`, `/rupert`, `/build-into-project`, `/thinking-partner`, …) → same: `/end-of-session` Steps 4–6 only.
- **Dean is closing the chat** → run `/end-of-session` in full.

## Redirects that still apply

- Raw idea → `/save-to-ideas`
- Task / to-do → **Zoho** (never tickboxes in `1 MORPh/`)
- Finished tracked project → `/finish-project`
- Non-MORPh (Academia / Personal) → `/update-to-obsidian`
